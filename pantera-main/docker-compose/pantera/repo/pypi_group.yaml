repo:
  type: "pypi-group"
  # Hosted member first: locally-uploaded packages take priority
  # over the proxy (PyPI.org). This ensures that a package uploaded
  # to the hosted repo is served from there, not from PyPI.org
  # which may have a different/empty package with the same name.
  members:
    - pypi
    - pypi_proxy
