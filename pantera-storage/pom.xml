<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.auto1.pantera</groupId>
        <artifactId>pantera</artifactId>
        <version>2.3.0</version>
    </parent>
    <artifactId>pantera-storage</artifactId>
    <version>2.3.0</version>
    <packaging>pom</packaging>
    <name>asto</name>
    <description>A simple Java storage</description>
    <url>https://github.com/auto1-oss/pantera/tree/master/asto</url>
    <inceptionYear>2019</inceptionYear>
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <header.license>${project.basedir}/../LICENSE.header</header.license>
    </properties>
    <modules>
        <module>pantera-storage-core</module>
        <module>pantera-storage-s3</module>
        <module>pantera-storage-vertx-file</module>
    </modules>
    <dependencies>
        <!-- tests -->
        <!-- TestNG compatibility layer with Junit5 -->
        <dependency>
            <groupId>com.github.testng-team</groupId>
            <artifactId>testng-junit5</artifactId>
            <version>0.0.1</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testng</groupId>
            <artifactId>testng</artifactId>
            <version>7.8.0</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>3.5.2</version>
                <configuration>
                    <forkCount>1</forkCount>
                    <reuseForks>true</reuseForks>
                    <useSystemClassLoader>true</useSystemClassLoader>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!--
                      FileStorage's async file I/O runs on ForkJoinPool.commonPool(),
                      whose default parallelism is (availableProcessors minus 1), i.e. 1
                      on a 2-core CI runner. Under the -T8 reactor (8 forks contending
                      for 2 cores) that single commonPool thread is starved, so the
                      multi-hop CachedBlobStorage save/read chains (FileStorage save,
                      digest, finalize; all commonPool hops) can wedge indefinitely on
                      save().join(): they pass on multi-core dev machines but time out
                      the CI job. Size the common pool for I/O concurrency so the storage
                      unit tests progress regardless of the runner's core count.
                      FileStorage now also has its own dedicated I/O executor
                      (mirroring OptimizedStorageCache BLOCKING_EXECUTOR), which is the
                      durable fix; this argLine remains as defense-in-depth against any
                      residual commonPool usage under -T8 on small runners.
                    -->
                    <argLine>-Djava.util.concurrent.ForkJoinPool.common.parallelism=8</argLine>
                </configuration>
            </plugin>
        </plugins>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>versions-maven-plugin</artifactId>
                    <version>2.12.0</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>