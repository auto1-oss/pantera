repo:
  type: "file-group"
  # Members are listed in priority order - first match wins
  members:
    - file           # local repository (highest priority)
    - file_proxy     # proxy to upstream
  # Optional: override global group cache settings
  settings:
    group:
      index:
        remote_exists_ttl: 15m
      metadata:
        ttl: 5m
